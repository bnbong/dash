**AppHeader** — screen header with optional mono eyebrow and trailing actions.

```jsx
<AppHeader large eyebrow="6월 30일 화요일" title="오늘"
  trailing={<IconButton icon={<i data-lucide="bell" />} label="알림" />} />
```
