**Switch** — on/off toggle, pine fill when on. Used in Profile/settings (e.g. 실명 공개, Maintenance Mode).

```jsx
<Switch label="실명으로 인증서 발급" defaultChecked={false} />
```
Controlled via `checked` + `onChange`, or uncontrolled via `defaultChecked`.
