**SeedBadge** — Impact Seed counter with the Dash sprout glyph. Ochre, never green; never described as money or points.

```jsx
<SeedBadge count={18} label="이번 달" />
<SeedBadge count="+1" variant="solid" size="sm" />
```
