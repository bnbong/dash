**Tag** — chip for filters and domain labels. Pine fill when selected; optional color `swatch` for domains.

```jsx
<Tag selected>전체</Tag>
<Tag swatch="var(--domain-body)" onClick={...}>Body</Tag>
<Tag removable onRemove={...}>취미</Tag>
```
