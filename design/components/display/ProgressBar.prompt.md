**ProgressBar** — calm meter, pine fill, rounded track. Used for Grove conversion and domain activity.

```jsx
<ProgressBar value={74} showLabel label="Community Grove 전환까지" />
<ProgressBar value={3} max={5} color="var(--domain-hobby)" />
```
